<UserControl x:Class="VWContractPrimaryApplicantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Aristo="clr-namespace:PBS.Framework.ControlLibrary.Wpf;assembly=PBS.Framework.ControlLibrary"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             xmlns:pbsForms ="clr-namespace:_3LAWMN12VW"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PBS.Framework.ControlLibrary;component/Wpf/AristoThemeDefault.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Height="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Grid.Row="0">
            <Label Content="{Binding MyCaption}" FontWeight="Bold" FontSize="14"/>
        </Border>
        <ContentControl x:Name="ApplicantName" Grid.Row="1" Grid.Column="0"></ContentControl>
        <!--<pbsForms:VWContractAddressView cal:View.Model="{Binding Address1}" Grid.Row="2" Grid.Column="0"></pbsForms:VWContractAddressView>
        <CheckBox Grid.Row="3" Content="Use Home Address for Mailing"></CheckBox>
        <pbsForms:VWContractAddressView cal:View.Model="{Binding Address2}" Visibility="{Binding DisplayAddress2}"  Grid.Row="4" Grid.Column="0"></pbsForms:VWContractAddressView>
        <CheckBox Grid.Row="5" Content="Use Home Address for Garage"></CheckBox>
        <pbsForms:VWContractAddressView cal:View.Model="{Binding Address3}" Visibility="{Binding DisplayAddress3}"  Grid.Row="6" Grid.Column="0"></pbsForms:VWContractAddressView>-->
    </Grid>
</UserControl>
